Esta carpeta está reservada para archivos intermedios del proceso de preprocesamiento de datos.

Actualmente, todo el preprocesamiento se realiza dentro del pipeline de scikit-learn en el notebook principal, por lo que no se generan archivos intermedios en esta carpeta.



Uso futuro

Esta carpeta podría utilizarse para:

- Datos preprocesados antes del modelado

- Datasets intermedios durante el proceso ETL

- Transformaciones específicas que requieran persistencia
